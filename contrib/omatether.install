# omatether post-install note.

post_install() {
  echo "==> Run 'omatether setup' to configure channels and start the service."
  echo "    It writes ~/.config/omatether/env and enables the user service."
}

post_upgrade() {
  post_install
}
